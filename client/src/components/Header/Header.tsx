import React from 'react';
import './style.css';
import { Nav } from '../Nav/Nav';

export const Header = () => {
  return (
    <header className="header">
      Header
      <Nav />
    </header>
  );
};
